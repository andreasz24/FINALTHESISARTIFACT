# data/

Place the raw dataset here:

    corporateCreditRatingWithFinancialRatios.csv

The file is not committed to the repository. Notebook 1 reads it from this
folder (or from the path set in the `THESIS_DATA_DIR` environment variable) and
writes the train/test split to `../results/splits.pkl`.
